# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# General TCP access inside Docker network
host    all             all             all                     scram-sha-256

# Streaming replication from replica container(s)
host    replication     scanflowread    all                     scram-sha-256
